# CommLab v2.8 Trade-off Matrix

| Question | Baselines / methods | Main metrics | Key caution |
|---|---|---|---|
| When should the server repair individual FL clients instead of broadcasting a full keyframe? | periodic keyframe; selective age; selective importance | weighted model age/MSE, fresh coverage, downlink size | selective repair loses when many chains fail together |
| Is an edge cache useful if its model version is stale? | popularity; latency value; version value; LRU | hit rate, task utility, model age, latency, backhaul | LRU can buy freshness with extreme churn |
| How much carbon reduction is compatible with persistent participation? | random; carbon-only; age bonus; virtual debt | loss, carbon proxy, Jain fairness, minimum participation | persistent fairness forces some high-carbon service |
| Should every low-confidence inference request enter the radio queue? | admit all; backlog gate; backpressure | on-time utility, deadline miss, admission fraction, backlog | value-based admission needs calibration under extreme overload |
| When should a physical process update its digital twin? | periodic full; error-triggered full; semantic delta | state RMSE, AoII, update fraction, radio load | semantic-delta gains depend on packet-size/outage and quantization model |
